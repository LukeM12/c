/*valid = create_queue(client, NULL, 56, temp, NULL);
   	valid = create_queue(client, NULL, 4, temp, NULL);
   	valid = create_queue(client, NULL, 8, temp, NULL);
   	EnQueue(client);
   	printf("Queue Length = %i \n", client->length);
    EnQueue(client);
    EnQueue(client);*/
    
    /*Sectioning off the unit testing modules. Above is length below is data */
    
    
    /*
   	valid = create_queue(client, NULL, port, temp, NULL);
   	valid = create_queue(client, NULL, 5, temp, NULL);
   	valid = create_queue(client, NULL, 3, temp, NULL);
   	valid = create_queue(client, NULL, 56, temp, NULL);
   	valid = create_queue(client, NULL, 4, temp, NULL);
   	valid = create_queue(client, NULL, 8, temp, NULL);
    PrintQueue(client);
    EnQueue(client);
    PrintQueue(client);
    EnQueue(client);
    EnQueue(client);
    EnQueue(client);
    PrintQueue(client);
    
    Unit testing minus the assert and makefile
    */
